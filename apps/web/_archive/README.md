# Archived web chrome

The multi-page studio / pipeline UI (DesignStudio, ribbon rails, costing/outputs hubs)
was superseded by the canvas-first product at `/projects/[id]` (July 2026 reboot).

Legacy components under `src/components/` may still exist for reference or portal reuse;
primary operator routes redirect to the site canvas.
