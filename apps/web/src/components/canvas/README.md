# Canvas

Live operator surface: `handoff/HandoffDesignStudio` (mounted from `/projects/[id]`).

`siteCanvas.module.css` remains for shared loading / home workflow chrome only.
Retired SiteCanvas / SketchInstrument / LiveBomHud modules were deleted — do not reintroduce them.
