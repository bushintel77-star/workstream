# GitLab CI is off. Pipelines burned minutes and took production down.
# Git remote stays this GitLab project. Deploy from the laptop:
#   railway up --service web|api --environment production --detach
# Quality gate: pnpm run ci (local only)
workflow:
  rules:
    - when: never
