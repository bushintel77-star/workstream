[build]
  base = "apps/web"
  command = "cd ../.. && pnpm --filter @workstream/web build"
  publish = "apps/web/.next"

[build.environment]
  NEXT_USE_NETLIFY_EDGE = "true"
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
