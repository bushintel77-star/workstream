name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Canvas-first reboot: public API on Railway.
  NEXT_PUBLIC_API_URL: https://api-production-a8ff1.up.railway.app

jobs:
  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6
        with:
          version: 9.15.4
          run_install: false

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm mobile:check-placeholders

      - run: pnpm mobile:check-distribution

      - run: pnpm web:check-portal-edge

      - run: pnpm web:check-handoff-colors

      - run: pnpm web:check-studio-dialect

      - run: node scripts/check-tier1-2026-spec-gap.mjs --require

      - run: pnpm web:check-reachability

      - run: pnpm web:check-css-scales

      - run: pnpm web:check-bundle-size

      - run: pnpm typecheck

      - run: pnpm lint

      - run: pnpm test

  e2e:
    name: playwright e2e (${{ matrix.shard }}/3)
    runs-on: ubuntu-latest
    # Sharded — a single worker on ~126 tests was ~30m and looked "hung" when
    # disabled controls burned the full 120s actionability wait (× retry).
    timeout-minutes: 35
    needs: typecheck
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    env:
      CI: true
      AUTH_REQUIRED: "false"
      API_URL: http://127.0.0.1:3001
      PLAYWRIGHT_BASE_URL: http://127.0.0.1:3002
      NEXT_PUBLIC_API_URL: http://127.0.0.1:3001
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 9.15.4
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install Playwright Chromium
        run: pnpm --filter @workstream/web exec playwright install chromium --with-deps
      - name: Build API workspace deps for e2e
        run: pnpm exec turbo run build --filter=@workstream/api...
      - name: Run Playwright shard
        run: pnpm --filter @workstream/web exec playwright test --shard=${{ matrix.shard }}/3

  build-api:
    name: build api docker image
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: typecheck
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v4
      - name: Build (no push)
        uses: docker/build-push-action@v7
        with:
          context: .
          file: apps/api/Dockerfile
          push: false
          tags: workstream-api:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-web:
    name: build web docker image
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: typecheck
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: apps/web/Dockerfile
          push: false
          tags: workstream-web:ci
          build-args: |
            NEXT_PUBLIC_API_URL=${{ env.NEXT_PUBLIC_API_URL }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
