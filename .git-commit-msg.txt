feat(web): canvas-first quote HUD, brand de-Curtis, buildable area overlay

Quote panel: page takeover to right-side frosted CameraChrome dock with
backdrop blur (canvas visible behind). Dense 36px rows, ellipsis-truncated
labels, right-aligned tabular-nums financials, hover-reveal action menu,
single pinned global header (no per-section repeats). Sans-serif throughout
(no monospace clash). Mobile: bottom sheet at 58vh.

Suggested mode: Survey to Sketch to CAD to Quote to Share (no skipping past Sketch).

Branding: removed Curtis and Co from all user-facing strings to Workstream
or natural language. Removed Tier-1, Removed/Deployed jargon to
Architectural massing, Simplified/Reinvested.

Buildable area: new domain module + canvas overlay (boundary minus setback,
building, easements, BYDA, TPZ, overlays with per-exclusion m2 attribution).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
