fix(ts): remove any from sentry.ts and mobile auth catch blocks

TypeScript scan found the only remaining real `any` annotations in the
repo outside comments:

- `apps/web/src/lib/sentry.ts`: replaced `any` with a narrow
  `SentryModule` interface, keeping the optional `import("..." as string)`
  pattern so the uninstalled dependency does not fail typecheck.
- `apps/mobile/app/(auth)/sign-in.tsx`: replaced `catch (err: any)` with
  `isClerkAPIResponseError(err)` from `@clerk/clerk-expo`.
- `apps/mobile/app/(auth)/sign-up.tsx`: replaced both `catch (err: any)`
  blocks with the Clerk guard.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
