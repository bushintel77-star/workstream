fix(web): convert present panel div to real button

The presentation panel used a `<div role="button" tabIndex={0}>` with a
hand-rolled `onKeyDown`. Replaced it with a native `<button type="button">`
so keyboard/screen-reader activation works without custom code, and reset
button default styling in CSS.

Also verified all 247 native `<button>` elements in `apps/web/src` already
have an explicit `type` attribute.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
