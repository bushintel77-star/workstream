fix(web): guard useChromeIdle from infinite update loop and clipboard permission errors

- useChromeIdle.ts: functional setIdle updates, useCallback for
  markActive/startIdleTimer, only re-render when idle actually flips.
  Previously setIdle(false) was called on every pointer/scroll/wheel
  event and on every effect re-run, causing a Maximum update depth
  exceeded loop.
- ShareSurface.tsx + ShareRevisionPopup.tsx: wrap
  navigator.clipboard.writeText in an inner try/catch so a
  NotAllowedError no longer surfaces as a runtime exception; the link
  is still generated and shown for manual copy.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
